spring:
  datasource:
    url: "jdbc:h2:mem:h2test;DB_CLOSE_DELAY=-1;MODE=MySQL"
    username: "sa"
    password: "sa"
    driver-class-name: "org.h2.Driver"
  h2:
    console:
      enabled: true
filter:
  login-filter:
    excludeURIList:
      - "login"
      - "image"
      - "register"
server:
  port: 8082
